{
  "description": "tottok memory hooks (SessionStart digest + Stop auto-capture, with idempotent tottok shim deployment)",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/install.sh\" --shim-only >/dev/null 2>&1 ; bash \"${CLAUDE_PLUGIN_ROOT}/scripts/run-cli.sh\" session-start"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/run-cli.sh\" stop"
          }
        ]
      }
    ]
  }
}
